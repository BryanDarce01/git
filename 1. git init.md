# `git init`

Sirve para inicializar un **nuevo repositorio Git** en una carpeta local.

- En otras palabras, **le dice a Git**:

    - “A partir de ahora, quiero que empieces a rastrear los cambios en esta carpeta”.

# ⚙️ ¿Qué hace exactamente?

Cuando se ejecuta `git init` dentro de una carpeta (por ejemplo, un proyecto), **Git**:

1. Crea una subcarpeta oculta llamada **.git/**

    - Esta carpeta contiene toda la información interna del repositorio, como:

    - Los commits

    - Las ramas (branches)

    - Los historiales de cambios

    - La configuración del repositorio

2. Marca la carpeta actual como un repositorio **Git local**.

3. Permite usar comandos como:

    -   `git add`

    -   `git commit`

    -   `git branch`

    -   `git push`

    -   etc.

## 💡 Nota:

Si se borra la carpeta **.git**, se pierde todo el historial de cambios (aunque los archivos del proyecto siguen ahí).